# WebCube 测试框架搭建方案

> 生成日期：2026-06-28 | 当前阶段：Phase 1 完成 | 目标：Phase 1 全覆盖 + Phase 2 基础 E2E
> **⚠️ 交互模型已变更**：鼠标拖拽仅旋转视角（Orbit），不再直接旋转魔方面。魔方转动需先点击选中某个正方形，再用 WASD/方向键操作。

---

## 一、交互模型说明（重要变更）

### 1.1 新交互模型

```
┌─────────────────────────────────────────────────────────────┐
│  步骤 1：鼠标拖拽 → 仅旋转视角（Orbit Camera）                │
│          不会触发任何魔方转动                                  │
│                                                             │
│  步骤 2：鼠标点击 → 选中魔方某个面的某个正方形                 │
│          （3×3 魔方，6 个面，每面 9 个正方形，共 54 个）       │
│          选中后该正方形高亮显示                                │
│                                                             │
│  步骤 3：键盘操作 → WASD / 方向键 → 旋转选中的正方形所在面     │
│          每个正方形只有 4 个操作方向：上/下/左/右              │
│          例如：选中右面(R)中心格 → W=顺时针 / S=逆时针         │
│                  选中上面(U)中心格 → A=左旋 / D=右旋          │
└─────────────────────────────────────────────────────────────┘
```

### 1.2 与旧模型的差异

| 操作 | 旧模型 | 新模型 |
|------|--------|--------|
| 鼠标拖拽魔方 | 直接旋转对应面 | **仅旋转视角（Orbit）** |
| 键盘 R/L/U/D/F/B | 直接旋转对应面 | **不再支持** |
| 点击魔方 | 无效果 | **选中某个正方形** |
| WASD / 方向键 | 无效果 | **旋转选中正方形所在面** |

### 1.3 对测试的影响

| 测试文件 | 影响 | 说明 |
|---------|------|------|
| `Interaction.test.ts` | **重写** | 拖拽旋转逻辑删除，改为点击选中 + WASD 旋转 |
| `keyboard-ops.spec.ts` | **重写** | R/L/U/D/F/B 测试删除，改为 WASD/方向键 + 点击选中 |
| `hud-buttons.spec.ts` | 不变 | HUD 按钮功能不变 |
| `cube-rendering.spec.ts` | 微调 | 需验证选中高亮效果 |
| `useGameStore.test.ts` | 不变 | 状态机逻辑不变 |

---

## 二、现状分析

### 2.1 已实现模块（Phase 1）

| 模块 | 文件 | 类型 | 可测性 |
|------|------|------|--------|
| **魔方状态机** | `src/stores/useGameStore.ts` | 纯逻辑 | ⭐⭐⭐⭐⭐ 极易测 |
| **3D 渲染** | `src/game/CubeRenderer.ts` | Three.js 类 | ⭐⭐⭐ 需 mock DOM |
| **交互层** | `src/game/Interaction.ts` | 事件驱动 | ⭐⭐ 需 mock DOM + 事件 |
| **画布初始化** | `src/components/Canvas.tsx` | Three.js 场景 | ⭐⭐ 需 mock DOM |
| **HUD UI** | `src/components/HUD.tsx` | DOM 操作 | ⭐⭐⭐ 可 E2E 测 |
| **首页** | `src/components/HomePage.ts` | DOM 操作 | ⭐⭐⭐ 可 E2E 测 |
| **设置面板** | `src/components/Settings.ts` | DOM 操作 | ⭐⭐⭐ 可 E2E 测 |
| **计时器** | `src/components/Timer.ts` | 纯逻辑 | ⭐⭐⭐⭐ 易测 |
| **应用入口** | `src/App.tsx` | 组装 | ⭐ 集成测试 |

### 2.2 测试策略分层

```
┌─────────────────────────────────────────────────────┐
│  E2E 测试 (Playwright)                               │
│  浏览器级：页面打开 → 点击选中 → WASD → 截图 → 验证   │
│  测：HUD 按钮、魔方渲染、选中+键盘操作、页面导航       │
├─────────────────────────────────────────────────────┤
│  组件测试 (Vitest + jsdom)                           │
│  DOM 级：HUD 创建、Settings 面板、HomePage 渲染       │
│  测：DOM 结构、事件绑定、状态响应                     │
├─────────────────────────────────────────────────────┤
│  单元测试 (Vitest)                                   │
│  纯逻辑：CubeState 转换、求解器、打乱、计时器          │
│  测：applyMove、undo/redo、scramble、checkSolved      │
└─────────────────────────────────────────────────────┘
```

---

## 三、依赖安装

### 3.1 单元测试 — Vitest

```bash
npm install -D vitest @vitest/coverage-v8 jsdom
```

- **Vitest**：与 Vite 原生集成，启动快，API 与 Jest 兼容
- **@vitest/coverage-v8**：代码覆盖率报告
- **jsdom**：模拟浏览器 DOM 环境，用于组件测试

### 3.2 E2E 测试 — Playwright

```bash
npm install -D @playwright/test
```

- **@playwright/test**：浏览器自动化测试框架
- 支持 Chromium / Firefox / WebKit 三浏览器
- 内置截图、视频录制、trace 录制

### 3.3 视觉回归 — Playwright 截图对比

```bash
npm install -D @playwright/test
# Playwright 内置 screenshot 对比，无需额外依赖
```

### 3.4 安装命令（一键）

```bash
npm install -D vitest @vitest/coverage-v8 jsdom @playwright/test
```

---

## 四、配置文件

### 4.1 Vitest 配置

**文件：`vitest.config.ts`**

```typescript
import { defineConfig } from 'vitest/config'
import path from 'path'

export default defineConfig({
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
      '@shared': path.resolve(__dirname, './shared'),
    },
  },
  test: {
    globals: true,
    environment: 'jsdom',
    include: ['src/**/*.test.ts', 'src/**/*.test.tsx'],
    exclude: ['**/node_modules/**', '**/dist/**', '**/e2e/**'],
    coverage: {
      provider: 'v8',
      reporter: ['text', 'html', 'json'],
      include: ['src/game/**', 'src/stores/**', 'src/components/**'],
      exclude: [
        'src/main.tsx',
        'src/App.tsx',
        'src/vite-env.d.ts',
        'src/types/**',
      ],
      thresholds: {
        lines: 60,
        branches: 50,
        functions: 60,
        statements: 60,
      },
    },
  },
})
```

### 4.2 Playwright 配置

**文件：`playwright.config.ts`**

```typescript
import { defineConfig, devices } from '@playwright/test'

export default defineConfig({
  testDir: './tests/e2e',
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 2 : 0,
  workers: process.env.CI ? 1 : undefined,
  reporter: 'html',
  use: {
    baseURL: 'http://localhost:5173',
    trace: 'on-first-retry',
    screenshot: 'only-on-failure',
    video: 'retain-on-failure',
  },
  projects: [
    {
      name: 'chromium',
      use: { ...devices['Desktop Chrome'] },
    },
  ],
  webServer: {
    command: 'npm run dev',
    url: 'http://localhost:5173',
    reuseExistingServer: !process.env.CI,
    timeout: 60000,
  },
})
```

### 4.3 Playwright 全局类型

**文件：`tests/support/global-setup.ts`**

```typescript
import { FullConfig } from '@playwright/test'

async function globalSetup(config: FullConfig) {
  // 确保 dev server 启动后页面完全加载
  const { request } = require('@playwright/test')
  const ctx = await request.newContext()
  await ctx.get(config.projects[0].use.baseURL + '/')
  await ctx.close()
}

export default globalSetup
```

---

## 五、测试文件结构

```
WebCube/
├── src/
│   ├── game/
│   │   ├── CubeRenderer.test.ts          # 3D 渲染单元测试
│   │   └── Interaction.test.ts           # 交互层单元测试（点击选中 + WASD）
│   ├── stores/
│   │   └── useGameStore.test.ts          # 状态机单元测试（核心）
│   ├── components/
│   │   ├── HUD.test.tsx                  # HUD 组件测试
│   │   ├── HomePage.test.ts              # 首页组件测试
│   │   ├── Settings.test.ts              # 设置面板组件测试
│   │   └── Timer.test.ts                 # 计时器单元测试
│   └── App.test.tsx                      # 应用集成测试
├── tests/
│   ├── e2e/
│   │   ├── cube-rendering.spec.ts        # 魔方渲染 E2E
│   │   ├── select-and-rotate.spec.ts     # 点击选中 + WASD 旋转 E2E（新增）
│   │   ├── hud-buttons.spec.ts           # HUD 按钮 E2E
│   │   ├── scramble-reset.spec.ts        # 打乱/重置 E2E
│   │   ├── undo-redo.spec.ts             # 撤销/重做 E2E
│   │   ├── timer.spec.ts                 # 计时器 E2E
│   │   └── home-page.spec.ts             # 首页导航 E2E
│   └── support/
│       └── global-setup.ts               # 全局设置
├── vitest.config.ts
├── playwright.config.ts
└── package.json
```

---

## 六、核心测试用例设计

### 6.1 单元测试 — `useGameStore.test.ts`（最重要）

**测什么**：魔方状态机，纯逻辑，零依赖。

```typescript
// 测试用例清单（共 ~30 个）

describe('applyMove', () => {
  test('R 顺时针旋转后，右面贴纸正确循环', ...)
  test('R 逆时针旋转后，右面贴纸正确循环', ...)
  test('R 旋转后，相邻面 U/F/D/B 的贴纸正确交换', ...)
  test('L/U/D/F/B 各面旋转后状态正确', ...)
  test('连续旋转后状态累积正确', ...)
})

describe('checkSolved', () => {
  test('初始还原状态返回 true', ...)
  test('打乱后返回 false', ...)
  test('还原后返回 true', ...)
})

describe('undo/redo', () => {
  test('undo 恢复到上一步状态', ...)
  test('redo 恢复到下一步状态', ...)
  test('undo 到底后不能再 undo', ...)
  test('redo 到头后不能再 redo', ...)
  test('新操作后 redo 历史被丢弃', ...)
})

describe('scramble', () => {
  test('打乱后状态不是还原状态', ...)
  test('打乱后 moveHistory 包含打乱步骤', ...)
  test('打乱后计时器重置', ...)
})

describe('resetCube', () => {
  test('重置后回到初始还原状态', ...)
  test('重置后 moveHistory 清空', ...)
  test('重置后计时器重置', ...)
})

describe('timer', () => {
  test('startTimer 后 isTimerRunning 为 true', ...)
  test('stopTimer 返回经过的时间', ...)
  test('resetTimer 清零', ...)
})

describe('hint', () => {
  test('requestHint 有操作历史时返回上一步的逆操作', ...)
  test('requestHint 无操作历史时返回默认提示', ...)
  test('超过 maxHints 后不再返回提示', ...)
})

describe('autoSolve', () => {
  test('autoSolve 后魔方还原', ...)
  test('autoSolve 后计时器停止', ...)
  test('autoSolve 后提示清除', ...)
})
```

### 6.2 单元测试 — `CubeRenderer.test.ts`

**测什么**：3D 渲染逻辑（需 mock Three.js）。

```typescript
// 测试用例清单（共 ~10 个）

describe('CubeRenderer', () => {
  test('创建后包含 27 个 cubelet', ...)
  test('getLayerCubelets 返回正确数量的 cubelet', ...)
  test('rotateLayer 旋转后逻辑位置正确更新', ...)
  test('rotateLayer 动画完成后 isAnimating 为 false', ...)
  test('highlightLayer 高亮后 emissive 正确设置', ...)
  test('reset 后所有 cubelet 回到初始位置和颜色', ...)
  test('dispose 后资源正确清理', ...)
})
```

### 6.3 单元测试 — `Timer.test.ts`

**测什么**：计时器纯逻辑。

```typescript
// 测试用例清单（共 ~8 个）

describe('Timer', () => {
  test('初始状态 isRunning 为 false', ...)
  test('start 后 isRunning 为 true', ...)
  test('stop 后 isRunning 为 false', ...)
  test('reset 后 elapsed 为 0', ...)
  test('formatTime 格式正确', ...)
  test('onUpdate 回调正确触发', ...)
})
```

### 6.4 组件测试 — `HUD.test.tsx`

**测什么**：HUD DOM 结构 + 事件绑定。

```typescript
// 测试用例清单（共 ~8 个）

describe('HUD', () => {
  test('创建后包含所有按钮', ...)
  test('timer 初始显示 0.000', ...)
  test('moveCount 初始显示 步数: 0', ...)
  test('点击打乱按钮触发 onScramble', ...)
  test('点击重置按钮触发 onReset', ...)
  test('点击撤销按钮触发 onUndo', ...)
  test('setMoveCount 更新显示', ...)
})
```

### 6.5 单元测试 — `Interaction.test.ts`（重写）

**测什么**：新交互模型——点击选中 + WASD/方向键旋转。

```typescript
// 测试用例清单（共 ~15 个）

describe('Interaction', () => {
  describe('点击选中正方形', () => {
    test('点击魔方表面后，选中对应正方形', ...)
    test('点击不同面的正方形，选中状态正确切换', ...)
    test('点击非魔方区域，不选中任何正方形', ...)
    test('选中后，该正方形高亮显示', ...)
  })

  describe('WASD 旋转（选中正方形后）', () => {
    test('选中右面(R)正方形 + W → 顺时针旋转右面', ...)
    test('选中右面(R)正方形 + S → 逆时针旋转右面', ...)
    test('选中上面(U)正方形 + A → 左旋上面', ...)
    test('选中上面(U)正方形 + D → 右旋上面', ...)
    test('选中前面(F)正方形 + W → 顺时针旋转前面', ...)
    test('选中前面(F)正方形 + S → 逆时针旋转前面', ...)
    test('选中左面(L)正方形 + A → 逆时针旋转左面', ...)
    test('选中左面(L)正方形 + D → 顺时针旋转左面', ...)
  })

  describe('方向键旋转（选中正方形后）', () => {
    test('选中正方形 + ↑ → 对应顺时针旋转', ...)
    test('选中正方形 + ↓ → 对应逆时针旋转', ...)
    test('选中正方形 + ← → 对应左旋', ...)
    test('选中正方形 + → → 对应右旋', ...)
  })

  describe('鼠标拖拽（仅旋转视角）', () => {
    test('拖拽魔方区域 → 视角旋转，魔方面不转动', ...)
    test('拖拽后魔方状态不变', ...)
  })

  describe('边界情况', () => {
    test('未选中正方形时按 WASD → 无操作', ...)
    test('选中正方形后点击其他正方形 → 切换选中', ...)
    test('选中正方形后按 R/L/U/D/F/B → 无操作（旧快捷键已废弃）', ...)
  })
})
```

### 6.6 E2E 测试 — `cube-rendering.spec.ts`

**测什么**：魔方在浏览器中正确渲染。

```typescript
// 测试用例清单（共 ~5 个）

describe('魔方渲染', () => {
  test('页面加载后 canvas 存在', ...)
  test('canvas 截图不为空', ...)
  test('魔方初始状态为还原（截图对比基准图）', ...)
  test('点击选中正方形后，该正方形高亮显示（截图对比）', ...)
})
```

### 6.7 E2E 测试 — `select-and-rotate.spec.ts`（新增，核心）

**测什么**：新交互模型——点击选中 + WASD/方向键旋转魔方。

```typescript
// 测试用例清单（共 ~12 个）

describe('点击选中 + 键盘旋转', () => {
  describe('选中正方形', () => {
    test('点击魔方右面中心格 → 该格高亮', ...)
    test('点击魔方上面中心格 → 该格高亮', ...)
    test('点击魔方前面中心格 → 该格高亮', ...)
    test('点击魔方左面中心格 → 该格高亮', ...)
    test('点击魔方下面中心格 → 该格高亮', ...)
    test('点击魔方后面中心格 → 该格高亮', ...)
  })

  describe('WASD 旋转', () => {
    test('选中右面 + W → 右面顺时针旋转（截图对比）', ...)
    test('选中右面 + S → 右面逆时针旋转（截图对比）', ...)
    test('选中上面 + A → 上面左旋（截图对比）', ...)
    test('选中上面 + D → 上面右旋（截图对比）', ...)
  })

  describe('方向键旋转', () => {
    test('选中右面 + ↑ → 右面顺时针旋转（截图对比）', ...)
    test('选中右面 + ↓ → 右面逆时针旋转（截图对比）', ...)
  })

  describe('边界情况', () => {
    test('未选中时按 WASD → 魔方无变化', ...)
    test('连续选中不同面 + 旋转 → 状态累积正确', ...)
  })
})
```

### 6.8 E2E 测试 — `hud-buttons.spec.ts`

**测什么**：HUD 按钮功能。

```typescript
// 测试用例清单（共 ~6 个）

describe('HUD 按钮', () => {
  test('点击打乱后魔方状态改变（截图对比）', ...)
  test('点击重置后魔方回到初始状态', ...)
  test('点击撤销后步数减少', ...)
  test('点击重做后步数增加', ...)
  test('点击提示后提示层高亮', ...)
  test('点击自动还原后魔方还原', ...)
})
```

### 6.9 E2E 测试 — `timer.spec.ts`

**测什么**：计时器功能。

```typescript
// 测试用例清单（共 ~4 个）

describe('计时器', () => {
  test('初始显示 0.000', ...)
  test('第一次操作后计时器开始', ...)
  test('重置后计时器归零', ...)
  test('空格键暂停/继续计时', ...)
})
```

### 6.10 E2E 测试 — `home-page.spec.ts`

**测什么**：首页导航。

```typescript
// 测试用例清单（共 ~3 个）

describe('首页', () => {
  test('首页显示三个模式卡片', ...)
  test('点击练习模式跳转到游戏页', ...)
  test('点击对战/协作模式记录选择', ...)
})
```

---

## 七、运行命令

### 7.1 添加脚本到 `package.json`

```json
{
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "server": "tsx server/index.ts",
    "test": "vitest",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:headed": "playwright test --headed"
  }
}
```

### 7.2 命令说明

| 命令 | 说明 |
|------|------|
| `npm run test` | 启动 Vitest watch 模式（改代码自动重跑） |
| `npm run test:run` | 运行一次所有单元测试 |
| `npm run test:coverage` | 运行单元测试 + 生成覆盖率报告 |
| `npm run test:e2e` | 运行 Playwright E2E 测试（自动开浏览器） |
| `npm run test:e2e:ui` | Playwright 可视化 UI 模式（可逐步调试） |
| `npm run test:e2e:headed` | 有头模式运行（能看到浏览器窗口） |

---

## 八、实施步骤

### Step 1：安装依赖

```bash
npm install -D vitest @vitest/coverage-v8 jsdom @playwright/test
npx playwright install chromium
```

### Step 2：创建配置文件

- `vitest.config.ts`
- `playwright.config.ts`
- `tests/support/global-setup.ts`

### Step 3：编写单元测试（优先）

按优先级顺序：
1. `useGameStore.test.ts` — 状态机，纯逻辑，最高优先级
2. `Timer.test.ts` — 计时器，纯逻辑
3. `CubeRenderer.test.ts` — 渲染逻辑
4. `Interaction.test.ts` — 交互层（点击选中 + WASD）
5. `HUD.test.tsx` — 组件测试
6. `HomePage.test.ts` — 组件测试
7. `Settings.test.ts` — 组件测试

### Step 4：编写 E2E 测试

按优先级顺序：
1. `cube-rendering.spec.ts` — 魔方渲染
2. `select-and-rotate.spec.ts` — 点击选中 + WASD 旋转（核心）
3. `hud-buttons.spec.ts` — HUD 按钮
4. `timer.spec.ts` — 计时器
5. `undo-redo.spec.ts` — 撤销/重做
6. `scramble-reset.spec.ts` — 打乱/重置
7. `home-page.spec.ts` — 首页

### Step 5：生成基准截图

E2E 测试中的截图对比需要基准图。首次运行：

```bash
npx playwright test --update-snapshots
```

### Step 6：验证

```bash
# 全量测试
npm run test:run
npm run test:e2e

# 覆盖率
npm run test:coverage
```

---

## 九、注意事项

### 9.1 代码问题（测试前需修复）

| 问题 | 文件 | 说明 |
|------|------|------|
| HUD 构造函数签名不一致 | `HUD.tsx` vs `App.tsx` | `HUD.tsx` 无参，`App.tsx` 传 `'practice'` |
| HUD 缺少方法 | `HUD.tsx` | `setTimerDisplay`, `setHintActive`, `setCallbacks` 未定义 |
| `@shared/types.js` 路径 | 多处 | tsconfig 中 `@shared` 映射到 `shared/`，但 import 用 `.js` 扩展名 |
| `CubeRenderer.updateFromState` | `CubeRenderer.ts` | 方法体为空，仅 `void` 占位 |
| **Interaction.ts 交互模型** | `Interaction.ts` | 需重写：拖拽→仅旋转视角，点击→选中正方形，WASD→旋转面 |

### 9.2 测试策略调整

- **单元测试先行**：纯逻辑测试写起来快，反馈快，能覆盖核心业务
- **E2E 测试后置**：需要 dev server 运行，启动慢，适合集成验证
- **截图基准图**：首次运行 E2E 时用 `--update-snapshots` 生成，之后对比检测回归
- **CI 集成**：后续可接入 GitHub Actions，每次 PR 自动跑测试

### 9.3 覆盖率目标

| 模块 | 目标覆盖率 |
|------|-----------|
| `useGameStore.ts` | 90%+ |
| `CubeRenderer.ts` | 70%+ |
| `Interaction.ts` | 70%+（新交互模型） |
| `HUD.tsx` | 70%+ |
| `Timer.ts` | 90%+ |
| 整体 | 60%+ |

---

## 十、总结

| 项目 | 说明 |
|------|------|
| **框架** | Vitest（单元测试）+ Playwright（E2E） |
| **测试文件** | ~15 个测试文件 |
| **测试用例** | ~90+ 个（含新增的点击选中 + WASD 测试） |
| **预估工作量** | 配置 + 编写：4-6 小时 |
| **AI 执行** | 全部由 AI 编写和运行，无需人工介入 |
| **后续扩展** | Phase 2 多人联机时，增加网络层 mock 测试 |
