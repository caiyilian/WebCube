# Electron Builder configuration — Phase 7.3
# https://www.electron.build/configuration

appId: com.webcube.app
productName: WebCube
copyright: Copyright © 2026 WebCube

directories:
  output: release
  buildResources: icons

files:
  - dist/**/*
  - dist-electron/**/*
  - package.json

# ── Windows ───────────────────────────────────────────────────
win:
  target:
    - target: portable
      arch:
        - x64
    - target: nsis
      arch:
        - x64
  icon: icons/icon.ico

portable:
  artifactName: WebCube-${version}-portable.${ext}

nsis:
  oneClick: false
  allowToChangeInstallationDirectory: true
  installerIcon: icons/icon.png
  uninstallerIcon: icons/icon.png
  license: LICENSE

# ── macOS ─────────────────────────────────────────────────────
mac:
  target:
    - target: dir
      arch:
        - x64
        - arm64
  icon: icons/icon.png
  category: public.app-category.games
  hardenedRuntime: true

# ── Linux ─────────────────────────────────────────────────────
linux:
  target:
    - target: dir
      arch:
        - x64
  icon: icons/icon.png
  category: Game
  maintainer: webcube@example.com
